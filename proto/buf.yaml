version: v1
name: buf.build/deltabye/hearth-media-server

lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME

breaking:
  use:
    - FILE

deps:
  - buf.build/googleapis/googleapis